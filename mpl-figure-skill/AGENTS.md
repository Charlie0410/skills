# Workspace Instructions

This repository is a local skill-development workspace for the `mpl-figure-generator` skill.

## Local Skill

- Treat `mpl-figure-generator` as an available local skill in this workspace.
- The canonical skill file is `skills/mpl-figure-generator/SKILL.md`.
- When the user asks to use, test, revise, package, or inspect this skill, open `skills/mpl-figure-generator/SKILL.md` and follow it.
- Load only the specific `assets/` and `references/` files needed for the current request.
- Prefer the workspace copy over any separately installed copy if they differ.
